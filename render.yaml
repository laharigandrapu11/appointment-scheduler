services:
  - type: web
    name: appointment-scheduler
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    branch: main
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod