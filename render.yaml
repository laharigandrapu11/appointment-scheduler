services:
  - type: web
    name: appointment-scheduler
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    branch: main
    envVars:
      - key: TZ
        value: America/New_York